class Team {
    constructor(){
        this.players = []
        this.curr_icon = 1
        this.score = 0
    }
}

module.exports = Team

//room.teams[0].players.length
//for num in players //objects //gives keys in object
//for num of players //iterate arrays //gives what's inside the arrays as in "a", "b", "c"
//for num in players //iterate arrays //gives you position as in 0,1,2